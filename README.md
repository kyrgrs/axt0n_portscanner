# Axton Port Scanner V1
This tool for IP to GEOLocation

# Installation
    pip install -r requirements.txt
    python axt0n_portscanner.py

# Usage
Run the program and enter the targets IP adress 
